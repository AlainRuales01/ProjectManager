import ProjectList from "./project_list";

const ProjectIndex = () => {

    return (
        <>
                {/* <button onClick={() => setProjects([])}>Add Projects</button> */}
            <ProjectList />
        </>
    );
}

export default ProjectIndex;